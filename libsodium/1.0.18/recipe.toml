[package]
name = "libsodium"
version = "1.0.18"

[source]
git = "https://github.com/jedisct1/libsodium.git"
tag = "1.0.18"

[build]
system = "cmake"
args = ["-DBUILD_TESTING=OFF", "-DBUILD_EXAMPLES=OFF"]
