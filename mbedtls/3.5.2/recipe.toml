[package]
name = "mbedtls"
version = "3.5.2"

[source]
git = "https://github.com/Mbed-TLS/mbedtls.git"
tag = "mbedtls-3.5.2"

[build]
system = "cmake"
args = ["-DBUILD_TESTING=OFF", "-DBUILD_EXAMPLES=OFF"]
