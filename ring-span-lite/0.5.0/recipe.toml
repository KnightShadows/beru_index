[package]
name = "ring-span-lite"
version = "0.5.0"

[source]
git = "https://github.com/martinmoene/ring-span-lite.git"
tag = "v0.5.0"

[build]
system = "cmake"
args = ["-DBUILD_TESTING=OFF", "-DBUILD_EXAMPLES=OFF"]
